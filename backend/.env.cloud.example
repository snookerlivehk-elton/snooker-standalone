NODE_ENV=production
# Railway 會自動注入 PORT，若使用 Railway 請勿手動設定
# PORT=3000

# 以逗號分隔允許的前端／Overlay 正式網址
# 例：你的自訂網域（含 www）
CORS_ORIGIN=https://snookerhk.live,https://www.snookerhk.live

# 連到雲端資料庫（PostgreSQL）
# 範例：postgresql://<user>:<password>@<host>:5432/<db>?schema=public
DATABASE_URL=postgresql://user:password@db-host:5432/snooker?schema=public

# Socket.IO 路徑（若有反向代理請同步設定）
SOCKET_IO_PATH=/socket.io

# 可選：啟用環境稽核與管理介面保護
ENV_AUDIT_ENABLED=true
ADMIN_TOKEN=wwww5678